import type { Session } from "@auth/core/types";

export const WelcomeToStreamerWars = ({ session, bgVolume, setBgVolume, bgAudio, isOpen, setIsOpen }: { session: Session, bgVolume: number, setBgVolume: (volume: number) => void, bgAudio: HTMLAudioElement, isOpen: boolean, setIsOpen: (isOpen: boolean) => void }) => {


}