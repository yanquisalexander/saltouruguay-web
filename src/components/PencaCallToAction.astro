<!-- ---
// PencaCallToAction.astro
---

<a
  href="https://penca.saltouruguayserver.com/"
  target="_blank"
  rel="noopener noreferrer"
  class="penca-cta"
  aria-label="Participar en la Penca del Mundial"
>
  {/* Borde animado */}
  <div class="penca-border" aria-hidden="true"></div>

  {/* Wrapper con overflow hidden para contener el zoom */}
  <div class="penca-img-wrapper">
    <img
      src="/images/penca-banner.webp"
      alt="La Penca del Mundial – SaltoUruguayServer"
      class="penca-img"
      draggable="false"
      loading="lazy"
    />
  </div>

  {/* Overlay hover */}
  <div class="penca-overlay" aria-hidden="true">
    <span class="penca-cta-label">
      ¡PRONOSTICA LOS RESULTADOS!
      <svg viewBox="0 0 20 20" fill="currentColor" width="18" height="18">
        <path fill-rule="evenodd" d="M10.293 3.293a1 1 0 011.414 0l6 6a1 1 0 010 1.414l-6 6a1 1 0 01-1.414-1.414L14.586 11H3a1 1 0 110-2h11.586l-4.293-4.293a1 1 0 010-1.414z" clip-rule="evenodd"/>
      </svg>
    </span>
  </div>
</a>

<style>
  .penca-cta {
    display: block;
    position: relative;
    width: 100%;
    max-width: 700px;
    margin: 0 auto;
    border-radius: 16px;
    padding: 2px;
    overflow: hidden;
    text-decoration: none;
    transition: transform 0.4s cubic-bezier(0.34, 1.56, 0.64, 1);
  }

  .penca-cta:hover {
    transform: translateY(-4px);
  }

  /* Borde giratorio */
  .penca-border {
    position: absolute;
    inset: 0;
    border-radius: 16px;
    padding: 2px;
    background: conic-gradient(
      from var(--angle, 0deg),
      #a855f7,
      #7c3aed,
      #fbbf24,
      #a855f7
    );
    -webkit-mask:
      linear-gradient(#fff 0 0) content-box,
      linear-gradient(#fff 0 0);
    -webkit-mask-composite: xor;
    mask-composite: exclude;
    animation: rotateBorder 4s linear infinite;
    opacity: 0.7;
    transition: opacity 0.3s;
    z-index: 1;
  }

  .penca-cta:hover .penca-border {
    opacity: 1;
    animation-duration: 2s;
  }

  @property --angle {
    syntax: "<angle>";
    initial-value: 0deg;
    inherits: false;
  }

  @keyframes rotateBorder {
    to { --angle: 360deg; }
  }

  /* Wrapper que recorta el zoom */
  .penca-img-wrapper {
    position: relative;
    z-index: 2;
    border-radius: 14px;
    overflow: hidden;
    width: 100%;
  }

  /* Imagen con zoom-out continuo (Ken Burns) */
  .penca-img {
    display: block;
    width: 100%;
    height: auto;
    border-radius: 14px;
    transform-origin: center center;
    animation: kenBurns 12s ease-in-out infinite alternate;
  }

  @keyframes kenBurns {
    0%   { transform: scale(1.0); }
    100% { transform: scale(1.08); }
  }

  /* Overlay con botón */
  .penca-overlay {
    position: absolute;
    inset: 0;
    z-index: 3;
    border-radius: 14px;
    display: flex;
    align-items: center;
    justify-content: center;
    opacity: 0;
    transition: opacity 0.3s ease;
  }

  .penca-cta:hover .penca-overlay {
    opacity: 1;
  }

  .penca-cta-label {
    display: inline-flex;
    align-items: center;
    gap: 8px;
    padding: 0.75rem 2rem;
    border-radius: 999px;
    background: linear-gradient(135deg, #a855f7, #7c3aed);
    color: white;
    font-weight: 700;
    font-size: 1.25rem;
    letter-spacing: 0.04em;
    box-shadow:
      0 0 30px rgba(168, 85, 247, 0.6),
      0 8px 20px rgba(0, 0, 0, 0.4);
    transform: translateY(8px);
    transition: transform 0.3s cubic-bezier(0.34, 1.56, 0.64, 1);
  }

  .penca-cta:hover .penca-cta-label {
    transform: translateY(0px);
  }
</style>
 -->