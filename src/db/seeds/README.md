# Database Seed Scripts

This directory contains seed scripts for populating the database with initial data.

## Pet Items Seed

To seed the pet items (food, toys, furniture, clothing, accessories):

```bash
npm run tsx ./src/db/seeds/pet-items.ts
```

Or using the migrator with manual seed calls in `seed.ts`.
