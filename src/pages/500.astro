---
import Layout from "../layouts/Layout.astro";

const preload = [
  {
    as: "image",
    href: "/images/404-salto.webp",
    type: "image/webp",
  },
];

const { isMaintenanceMode } = Astro.locals;

const title = isMaintenanceMode ? "Mantenimiento" : "Error interno";
---

<Layout title={title} preload={preload} hideHeader>
  <div class="min-h-screen flex flex-col justify-center items-center h-full">
    <header
      class="flex items-center justify-center max-w-md w-full gap-x-14 gap-y-6 flex-col"
    >
      <div class="relative">
        <img
          draggable="false"
          src="/images/500.webp"
          alt="This is fine"
          class="h-48 sm:h-56 w-auto object-contain"
        />
      </div>
      <h1 class="text-2xl font-bold text-center flex-wrap">
        {isMaintenanceMode ? "¡Estamos en mantenimiento! 🚧" : "¡Upps! 😢"}
      </h1>
    </header>

    <p
      class="font-rubik uppercase text-lg text-balance text-center mt-6 max-w-xl"
    >
      <span class="font-bold text-yellow-500"> ¡Upps! </span>
      <br />
      No te preocupes, nuestros gatos ingenieros ya descansaron y están trabajando
      para solucionar el problema. <br />
    </p>
  </div>
</Layout>
