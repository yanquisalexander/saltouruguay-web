import { client } from "@/db/client";
import { SaltoTagAchievementsTable, SaltoTagsTable, UsersTable } from "@/db/schema";
import { eq } from "drizzle-orm";

export const getSaltoTag = async (userId: string) => { }
